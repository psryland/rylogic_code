//**********************************
// Extract
//  Copyright (c) Rylogic Ltd 2015
//**********************************

#pragma once
#include <type_traits>

namespace pr
{
	namespace str2
	{
		//template <typename Str, typename Char = decltype(*&Str()[0])> inline 

		//// Read a contigous block of characters, upto (and optionally including) a new line character
		//template <typename Str, typename Ptr, typename Char = decltype(*Ptr())> inline bool Line(Str& line, Ptr& src, bool inc_cr, Char const* newline = nullptr)
		//{
		//	// Use the default newline sequence "\n"
		//	Char const nl[] = {'\n',0};
		//	if (newline == nullptr) newline = &nl[0];

		//}
	}
}