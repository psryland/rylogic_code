<UserControl
	x:Class="LDraw.UI.SourcesListUI"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
	xmlns:local="clr-namespace:LDraw.UI"
	mc:Ignorable="d" 
	d:DesignHeight="450"
	d:DesignWidth="800"
	HorizontalAlignment="Stretch"
	VerticalAlignment="Stretch"
	Padding="0"
>
	<UserControl.Resources>
		<ContextMenu x:Key="SourcesListCMenu">
			<MenuItem
				Header="Add Source..."
				Command="{Binding AddSource}"
			/>
			<MenuItem
				Header="Edit..."
				Command="{Binding OpenInEditor}"
				IsEnabled="{Binding OpenInEditor.Available}"
			/>
		</ContextMenu>
	</UserControl.Resources>
	<ListBox
		ItemsSource="{Binding Sources}"
		SelectedItem="{Binding Sources/, Mode=TwoWay}"
		ContextMenu="{StaticResource SourcesListCMenu}"
		ItemContainerStyle="{StaticResource NoPaddingListBoxItem}"
		ScrollViewer.HorizontalScrollBarVisibility="Disabled"
		HorizontalContentAlignment="Stretch"
		BorderThickness="0"
	/>
</UserControl>
