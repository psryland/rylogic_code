Add/Remove lexers by moving them between this directory and the 'lexers_unused' directory.
The LexGen.py will need to be run, however this should be set up as a prebuild step in VS.
Same with HFacer.py