<resources>
    <!-- Base application theme. -->
    <style name="Theme.AllKeys" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_700</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>

        <!-- Customize your theme here.
        -->
    </style>


    <style name="Theme.AllKeys.Light" parent="Theme.AllKeys" />
    <style name="Theme.AllKeys.Dark" parent="Theme.Material3.Dark.NoActionBar" />
    <!--
    <style name="Theme.AllKeys.NoActionBar">
        <!- Primary brand color.
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        ->
    </style>

    <style name="Theme.AllKeys.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="Theme.AllKeys.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
    -->
</resources>